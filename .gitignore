# Environments / caches
.venv/
__pycache__/
*.pyc
# NOTE: uv.lock is intentionally NOT ignored — commit it to pin extra versions.

# Large CRAG data download — CC BY-NC 4.0, fetch separately (see STUDY.md); never
# commit. Also covers the regenerable synthetic fixture (make_fixture.py).
*.jsonl.bz2

# Root-level scratch outputs from smoke tests. The canonical results that back the
# paper live in results/ and ARE committed; these top-level ones are throwaway.
/stabilization.csv
/latency_validation.csv
/stabilization.summary.json
/phi_distribution.png

# LaTeX build intermediates (keep main.tex / results.tex / refs.bib + main.pdf)
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.fdb_latexmk
paper/*.fls
paper/*.synctex.gz
